using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowCar : MonoBehaviour
{
    public GameObject steeringWheelAttachPoint;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //transform.position = steeringWheelAttachPoint.transform.position;
        //transform.rotation = steeringWheelAttachPoint.transform.rotation;
    }
}
